import { ApplicationStatus } from './application-status.model';

describe('ApplicationStatus', () => {
  it('should create an instance', () => {
    expect(new ApplicationStatus()).toBeTruthy();
  });
});
